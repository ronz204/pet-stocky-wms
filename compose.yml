name: graphql

include:
  - path: ./docker/database/compose.yml

volumes:
  graphql-data:
    driver: local
    name: graphql-data

networks:
  graphql-network:
    driver: bridge
    name: graphql-network
